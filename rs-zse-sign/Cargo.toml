[package]
name = "zse_signer"
version = "0.1.0"
edition = "2021"

[lib]
name = "zsesigner"
crate-type = ["cdylib"]

[dependencies]
jni = "0.21.1"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = "symbols"

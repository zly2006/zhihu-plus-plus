# KMP Migration Status

## Completed

- Project has `shared`, `desktopApp`, Android `app`, and Android-only `sentence_embeddings`.
- `desktopApp` is a shallow demo1-style launcher.
- QR login core and QR UI are shared via `SharedQrLoginPane`.
- Android login uses shared QR login; Android WebView stays in app.
- JVM login uses shared QR login and stores cookies under `~/.zhihu-plus-plus/account.json`.
- KMP Room is used for content filter and local content databases.
- 共享导航语义应由 `shared/commonMain` 拥有；`NavDestination`、`LocalNavigator.kt`、`AnswerNavigator.kt` 已迁回 shared。`AnswerNavigator` 的 Android 数据访问通过 `AndroidAnswerNavigatorRepository` 留在 Android 平台适配层。
- `ZhihuMain.kt` 主导航壳已通过 `git mv` 迁入 `shared/commonMain`；但 route 注册仍需按 `master` 的大函数形状收回 shared。Android 只应保留 `MainActivity`、偏好读取、ViewModel 创建和其他运行时副作用 adapter，不得保留完整页面实现。
- `ThemeManager` / `ZhihuTheme` 的主题状态和 Material3 主题壳已迁入 `shared/commonMain`；Android 持久化、system dark、dynamic color 和 system bar 副作用留在 `shared/androidMain` adapter。
- Bottom navigation preference keys and normalization rules are shared in `shared/commonMain`; Android preference screens and `ZhihuMain` adapters reuse that common rule set.
- Account session data and JSON persistence rules have a shared repository; Android and JVM desktop storage are thin file-path adapters over that repository.
- Feed display mapping is shared via `Feed.toDisplayItem`; Android feed view models only pass platform preferences into the shared mapper.
- `PaginationViewModel.kt` 本体已通过 `git mv` 迁入 `shared/commonMain`，当前 common API 依赖 `PaginationEnvironment` 而不是 Android `Context`。Android 侧仍有临时 `Context -> PaginationEnvironment` 薄适配，feed/comment/list 子类还在 Android source set；下一步继续把子类的状态和纯分页流程迁入 shared，并把登录过期 Dialog、Toast/clipboard、Activity 导航、偏好读取、history repository 等副作用压缩到平台 adapter。不得用 `ZhihuPageLoader` 替代本体迁移。
- `CollectionsViewModel`、`CollectionContentViewModel` 和 `CollectionContentScreen` 已迁入 `shared/commonMain`。收藏夹分页、展示状态、导出进度状态、item 映射和页面主体属于 shared；收藏夹信息 fetch、详情解析、HTML/ZIP 文件导出、Toast/log、Android 文件路径、cacheDir、文章 host 和回答导航仓库由平台 adapter 提供。
- `HomeScreen`、`FollowScreen`、`HotListScreen`、`PeopleScreen`、`QuestionScreen`、`SearchScreen` 当前已去掉对 app `MainActivity` / `LoginActivity` / `WebviewActivity` / app `TopLevelReselectAction` typealias 的直接依赖，并且页面主体已迁入 `shared/commonMain`。本地推荐相关 Android 实现已先移入 `shared/androidMain` 作为临时桥，仍需继续拆 DB/client/connectivity/message/log environment 并迁 shared。
- `PinScreen` 页面主体已迁入 `shared/commonMain`，想法 UI、关联内容卡片、分享/评论入口和点赞展示复用同一套 Compose；详情加载、点赞请求、WebView/Markdown 渲染、分享/评论弹层、外链打开和 open event 记录由 Android/JVM runtime adapter 承接。`CommentScreen.kt` 已通过 `git mv` 进入 `shared/androidMain`，清掉 shared 对 app UI/ViewModel 的反向依赖；评论状态机和 UI 仍应继续拆副作用后迁 common。
- `WebviewComp` 保持 Android-only WebView 实现留在 `shared/androidMain`，但已去掉 app `MainActivity` / `WebviewActivity` 类型依赖，改用 className 启动和最小 runtime navigation adapter；WebView 本体不得迁 common，URL 到 `NavDestination` 的语义后续可继续抽 shared policy。
- `DeveloperSettingsScreen` 页面主体已迁入 `shared/commonMain`，并删除对应整页 `expect/actual`；开发者模式偏好、cookie 保存、登录校验、token 刷新、签名请求复制、网络/省电状态、Toast 和 Android `TextToSpeech` runtime 信息收敛到 `DeveloperSettingsRuntime` 平台 adapter。
- `AppearanceSettingsScreen` 页面主体和 `ColorPickerDialog` 已迁入 `shared/commonMain`，并删除对应整页 `expect/actual`；设置读写复用 common `SettingsStore`，提示复用 `UserMessageSink`，主题写入和 WebView 自定义字体文件选择收敛到小粒度 runtime adapter。
- `SystemAndUpdateSettingsScreen` 页面主体已迁入 `shared/commonMain`，并删除对应整页 `expect/actual`；GitHub token、Nightly、遥测、防沉迷间隔等设置语义复用 common `SettingsStore`，更新状态与 APK 下载/安装、外链打开收敛到 Android/JVM runtime adapter。Discord/Telegram/GitHub 图标迁入 Compose common resources；Android 侧保留账号页仍引用的 GitHub drawable。
- `AccountSettingScreen` 页面主体已迁入 `shared/commonMain`，并删除对应整页 `expect/actual`；账号状态、profile 刷新、登录/扫码、退出登录、复制版本号、外链打开、主 tab 选择和更新消息收敛到 `AccountSettingsRuntime` 平台 adapter。launcher/license 图标迁入 Compose common resources，桌面端复用同一账号页 UI。
- `BlocklistSettingsScreen` 页面主体已迁入 `shared/commonMain`，并删除对应整页 `expect/actual`；关键词/用户/主题列表 UI、统计卡片、添加/删除/清空对话框和 NLP slot 复用同一套 Compose，Android/JVM 只通过 `BlocklistSettingsRuntime` 提供数据库服务、导入/导出文件能力和平台消息。
- `BlockUserConfirmDialog` 的确认对话框 UI 已迁入 `shared/commonMain`；Android actual 只保留用户屏蔽写入和 Toast，JVM actual 复用同一对话框 UI 且不引入 Android 依赖。
- `HighlightableSettingContainer`、`VerticalReadingProgressBar` 和 `simpleVerticalScrollbar` 等纯 Compose 组件已迁入 `shared/commonMain`，不再作为 Android UI 组件债务保留。
- `AnswerHorizontalOverscroll` 和 `AnswerVerticalOverscroll` 已迁入 `shared/commonMain`；回答切换手势、阻尼、预览卡片和 haptic 触发状态属于 shared 文章交互 UI，平台 WebView 仍只作为外部 slot 内容传入。
- `ShareDialogContent` 已迁入 `shared/commonMain`；分享弹层视觉结构、按钮布局和动画复用同一套 UI，Android wrapper 仅保留系统分享、剪贴板、偏好读取和导航设置入口等平台副作用。
- `HomeAccountSettingSheetContent` 整页 expect/actual 转发壳已删除；首页 Duo3 账号底 sheet 直接调用 common `AccountSettingScreen`。
- `OpenSourceLicensesContent` 已迁入 `shared/commonMain`；Android/JVM 只保留 aboutlibraries 数据加载和 full/lite 手动许可显示判断的小粒度 adapter，desktop 不再显示许可列表迁移占位文案。
- `BlockByKeywordsDialog` 外层状态机已迁入 `shared/commonMain`；关键词提取和 NLP 短语写入收敛到 `BlockByKeywordsRuntime` 平台 adapter，Android 保留真实 NLP/数据库能力，JVM 不引入 Android 依赖。
- `OpenImagePreviewContent` 已迁入 `shared/commonMain`；黑色全屏图片预览、点击关闭、长按菜单、菜单位置和菜单文案由 common 复用，Android `OpenImageDialog` 只保留 `ComponentDialog` 宿主、Telephoto 图片 renderer slot 和保存/分享/外链副作用。
- `CommentScreenSheetContent` 和 `CommentScreenComponent` 已迁入 `shared/commonMain`；评论 root/child bottom sheet 编排、sheet state、标题拖拽柄、回复 sheet 切换和评论页面 wrapper 由 common 复用。
- `SegmentedText` 已迁入 `shared/commonMain`；划线文本渲染、虚线高亮、action sheet、selection 状态和 `SegmentCommentHolder` 映射复用同一套 UI，Android/JVM 只通过 `SegmentedTextRuntime` 提供复制和点赞能力，字体/行高读取改用 common `SettingsStore`。
- `BaseCommentViewModel`、`RootCommentViewModel` 和 `ChildCommentViewModel` 已迁入 `shared/commonMain`；评论分页、排序、去重、提交和点赞状态复用 common ViewModel，签名请求、HttpClient 和错误处理入口通过 `PaginationEnvironment` 注入。
- `CommentScreen` 页面主体已迁入 `shared/commonMain`；评论列表、排序、回复输入、滑动回复、作者跳转、点赞状态、图片菜单 UI、时间展示和 test tags 复用同一套 Compose。Android/JVM 通过 `CommentScreenRuntime` 提供图片打开/保存/分享、外链打开、emoji inline content 和选择菜单 workaround；Android 继续复用现有 `OpenImageDialog`、相册保存、系统分享和 Honor workaround，JVM 不引入 WebView。
- `ArticleUiCommon` 已迁入 `shared/commonMain`；文章页投票按钮颜色、底栏避让 bring-into-view 规则和 `ArticleTopAppBar` 等纯 UI helper 不再留在 Android source set。`ArticleScreen` 页面主体和 `ArticleViewModel` 仍在 `shared/androidMain`，后续继续拆 WebView/TTS/分享/导出/收藏等平台副作用。
- `HistoryViewModel`、`OnlineHistoryViewModel` 和 `OnlineHistoryScreen` 的本地历史读取/清理已改为直接使用 `shared/androidMain` 的 `HistoryStorage`，不再 cast `MainActivity.history`。
- `ContentFilterManager` is in `shared/commonMain`. `ContentFilterExtensions` has moved from app into `shared/androidMain` as the current Android wrapper over shared filter pipelines; its remaining Android `Context`/preferences/Room/log/Toast/detail-fetch dependencies are adapter seams, not ownership. Continue moving the orchestration and strategy into `shared/commonMain` instead of replacing or bypassing filtering logic.
- `ContentFilterSettingsScreen` should not use a mixed page adapter for all platform work. Its common bridge is split into generic `SettingsStore`, `ContentFilterMaintenance`, and `UserMessageSink` capabilities; filter maintenance owns one shared KMP Room-backed implementation instead of copying Android DAO logic in platform actuals.
- Newly extracted generic adapters must be reused in the same migration slice where reasonable. Grep for duplicate `Toast.makeText`, `getSharedPreferences`, and filter maintenance logic; replace low-risk call sites with `UserMessageSink`, `SettingsStore`, and `ContentFilterMaintenance`, or record why a remaining Android-only call site is deliberately left alone.
- `DailyScreen`、`CollectionScreen`、`NotificationScreen`、`OpenSourceLicensesScreen` 页面主体已迁入 `shared/commonMain`；Android 仅保留必要 data/provider/runtime adapter。`SentenceSimilarityTestScreen` 不是普通 shared 页面，full/lite 变体实现必须留在 Android app variant。
- `HotListScreen`、`HotListViewModel`、`BaseFeedViewModel` 的共享分页/展示主体以及 `FeedCard`、`PaginatedList`、`FeedPullToRefresh`、`DraggableRefreshButton`、`AuthorBadge` 已迁入 `shared/commonMain`。Android 屏蔽用户/关键词/主题的详情抓取、Toast、Blocklist 写入保留为 `shared/androidMain` extension；Android/JVM 分别提供分页环境、variant capability、屏幕尺寸、官方徽章图标和 HTML 文本解析 adapter。desktop 登录后已改为调用 shared `HotListScreen`，不再使用单独的 JVM 热榜 UI。
- `FollowViewModel`、`FollowRecommendViewModel`、`RecentMomentsViewModel` 和 `FollowScreen` UI 主体已迁入 `shared/commonMain`，关注页推荐/动态分页状态、recent moments 用户列表状态、JSON 解码和页面结构不再依赖 Android `Context`/`AccountData`/`Log`。屏蔽用户确认、主题屏蔽、详情补齐和 Toast 继续由 Android feed block action adapter 承接，JVM 端提供空能力以避免引入 Android/WebView 依赖。
- `HomeScreen` 页面主体已迁入 `shared/commonMain`，首页 top bar、公告、feed 列表、刷新和 reselect 行为复用同一套 UI；Android 账号状态/登录跳转、更新公告、本地推荐反馈、外部链接、调试复制和屏蔽关键词等副作用收敛到小 adapter，JVM 端至少复用 shared `HomeFeedViewModel`，且不引入 Android/WebView 依赖。
- `SearchViewModel` 已迁入 `shared/commonMain`，搜索分页状态、搜索结果解码和 `SearchResult -> Feed` 映射复用 common；搜索 query 编码改用 Ktor common `encodeURLParameter(spaceToPlus = true)`，不再依赖 Java `URLEncoder`。
- `HistoryViewModel`、`OnlineHistoryViewModel` 已迁入 `shared/commonMain`；本地历史列表读取通过 `PaginationEnvironment.localHistory()` adapter 提供。Android 仍由 `HistoryStorage(context)` 负责文件路径和持久化，JVM/desktop 暂返回空本地历史，不影响在线历史分页和 common 显示模型。
- `QuestionFeedViewModel` 已迁入 `shared/commonMain`，问题页回答分页、排序状态和 answer feed 显示映射复用 common；关注/取消关注问题的 POST/DELETE 副作用通过 `PaginationEnvironment.followQuestion()` 由 Android adapter 执行。
- `HomeFeedViewModel` 已迁入 `shared/commonMain`，首页 Web 推荐分页、前台/后台过滤编排、read/touch 上报触发、内容交互记录触发和展示列表合并逻辑复用 common。Android adapter 通过 `PaginationEnvironment` 继续承接实际 `ContentFilterExtensions` 调用、`lastread/touch` HTTP 请求、屏蔽/已读数据库写入和偏好读取；本地/Android/Mixed 首页推荐 ViewModel 改为实现 common `HomeFeedInteractionViewModel`，不再依赖 Android UI 文件里的接口。
- `LegacyLocalHistoryScreen` 页面主体已迁入 `shared/commonMain`，并删除对应整页 `expect/actual`；Android/JVM 共用同一个本地历史列表 UI，平台差异只由 `PaginationEnvironment.localHistory()` adapter 提供。
- `SearchScreen` 页面主体已迁入 `shared/commonMain`，并删除对应整页 `expect/actual`；热搜请求改为通过 common `PaginationEnvironment.fetchJson` 和 `ZhihuJson` 解码，搜索结果分页继续复用 common `SearchViewModel`。
- `OnlineHistoryScreen` 页面主体已迁入 `shared/commonMain`，并删除对应整页 `expect/actual`；清除在线/本地历史副作用通过 `PaginationEnvironment.clearAllHistory()` 由 Android adapter 执行，更多菜单的返回键处理改为小粒度 `PlatformBackHandler` adapter。
- `shared/src/androidMain` 中现存任何完整 Compose UI 页面、整页导航壳或整页 `expect/actual` 都只是迁移债务，不是可接受终态。Android source set 只能保留小粒度平台 adapter/provider/slot；页面主体、screen 结构、导航图和跨平台 UI 运行语义必须继续迁入 `shared/commonMain`。
- Shared has feed data models, notification/daily/hot-list/read-history clients, display formatting, ZSE signing, and local recommendation scoring helpers.

## Do Not Redo

- 不得留下仍存活的 subagent 继续自行决策。只要本轮启动、接手上下文、摘要或环境中列出的任何 subagent 仍然存活，主 agent 必须继续 `wait_agent` 等待完成；subagent 是前台阻塞任务，不是可遗留后台任务。在此之前不能提交、不能宣布完成、不能发送最终回复、不能把本地判断当成最终结论，不能修改代码或文档，也不能继续推进任何实现或迁移工作。等待完成并消费结论是唯一默认路径，且优先级高于并行推进、节省时间、本地判断、继续实现或提交切片；不能因为本地判断似乎足够、等待较久、或认为待返回结论大概率不重要而绕过。`wait_agent` 超时、返回空状态、或没有明确 `completed` 结论时，一律按 subagent 仍存活处理，应继续等待、补充输入或调整任务，不能一边等待一边做会影响结论的本地工作。主动关闭不是常规退路；只有用户明确取消、任务已作废、或书面说明该结果已不再可能影响当前工作时才能关闭，并必须记录原因。
- Do not keep navigation semantics or the main navigation shell Android-only. Move shared route/destination semantics plus `ZhihuMain.kt`, `LocalNavigator.kt`, and `AnswerNavigator.kt` toward `shared/commonMain`; keep only Android runtime side effects (`Context`, `Intent`, WebView, APK/update/install semantics, platform-only callbacks) in app.
- Do not move `ZhihuMain` route registration into an Android-only helper such as `androidZhihuMainRouteContent`. Match `master`: the shared `ZhihuMain` big function owns `NavHost` and all `composable<...>` route registrations; platforms inject runtime side effects only, not whole page implementations.
- Do not keep complete UI code in `shared/src/androidMain`. Full screens, complete Compose page bodies, whole navigation shells, and page-level `expect/actual` implementations must be moved to `shared/commonMain`; `expect/actual` is only for small platform capabilities such as Context/Intent/WebView/Activity, Toast/Dialog/notifications, file pickers, link opening, database builders/file paths, settings persistence, dynamic color/system dark, and TTS engines.
- Use `org.jetbrains.androidx.navigation:navigation-compose` as the preferred KMP navigation runtime. The current Android module already depends on `org.jetbrains.androidx.navigation:navigation-compose:2.9.2`; continue by moving that dependency to shared/commonMain and validating JVM/desktop compilation before introducing any custom route adapter.
- Do not assume desktop needs a separate route model. Desktop should reuse the shared Android UI/navigation semantics for this migration; only introduce a thin runtime adapter if the current Navigation Compose dependency cannot compile for JVM/desktop.
- Do not recreate `Time.android.kt` / `Time.jvm.kt`; use `Clock.System`.
- Do not put APK/lite/full/update/install semantics into `shared`.
- Do not make `desktopApp` contain QR login, cookie persistence, networking, or main UI state.
- Do not recreate the removed `.codex/hooks.json`; that deletion is intentional for this worktree.
- Do not introduce `ZhihuPageLoader` or another one-off loader layer as a substitute for migrating `PaginationViewModel`. Split side effects into small cross-platform interfaces/adapters and move the ViewModel.
- Do not move theme mode/custom color/background color state back to app just because Android currently owns persistence or dynamic color. Only platform environment and side effects stay in adapters.
- Do not treat `ContentFilterManager` or `ContentFilterExtensions` as Android-only just because they currently read Android preferences, Room builders, or `Context`; move the filtering orchestration and strategy to shared and keep only platform access in adapters.
- Do not grow a page-specific content-filter-settings aggregate back into a mixed adapter. Keep `SettingsStore`, `ContentFilterMaintenance`, and `UserMessageSink` separate and generically named; page-level adapters must not become a replacement for the shared filtering manager.
- Do not leave newly extracted generic adapters unused outside their first page when adjacent low-risk duplicates exist.

## Remaining Work

- Continue shrinking the Android `ZhihuMain` adapter only where it removes real platform side effects; do not rewrite the shared main shell.
- Audit and eliminate all remaining complete UI bodies in `shared/src/androidMain`, including page-level `expect/actual` bridges; move UI bodies to `shared/commonMain` and leave only minimal platform capability adapters.
- Split account fetch/token refresh orchestration from Android `AccountData`.
- Continue migrating `PaginationViewModel` subclasses and Android-only pagination call sites into shared after splitting their remaining platform side effects into small adapters.
- Continue migrating the remaining Android-only page bodies now that `BaseFeedViewModel` and shared feed card/list components are common; keep Android block-list/content-detail side effects as small adapters until the filter pipeline is shared.
- Move `ContentFilterManager` and `ContentFilterExtensions` into shared after splitting preferences, database builder, logging/toast, and content-detail fetch side effects into adapters.
- Add a shared hot-list/home shell that Android and desktop can both invoke.
- Move local recommendation orchestration behind platform adapters.
- Move pure HTML/text parsing only after replacing Jsoup with Ksoup-compatible shared code.
- Run Android AVD and JVM QR login runtime validation before final completion.

## Latest Verification

- 2026-05-21：`./gradlew :shared:compileAndroidMain --continue` 通过。
- 2026-05-21：`./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin --continue` 通过。
- 2026-05-21：`./gradlew assembleLiteDebug` 通过。
- 2026-05-21：`./gradlew ktlintFormat` 已执行 app/main、desktop、shared/commonMain、shared/androidMain 格式化任务，但最终被 iOS 编译任务拦下；失败点仍是本迁移期已知的 iOS/common 不兼容项（`JvmInline`、`Integer`、`System`、`java`、`String.format` 等）。本阶段仍不执行 iOS 修复。
- 2026-05-22：HotList common UI 切片通过 `./gradlew assembleLiteDebug`。
- 2026-05-22：HotList common UI 切片通过 `./gradlew :shared:jvmTest :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`。
- 2026-05-22：HotList common UI 切片通过 `./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet :app:runKtlintFormatOverMainSourceSet :desktopApp:runKtlintFormatOverMainSourceSet`。总任务 `./gradlew ktlintFormat` 仍会继续触发并失败于既有 iOS 编译问题（同 2026-05-21 记录），本阶段不处理 iOS。
- 2026-05-22：Follow ViewModel common 切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，并通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet`。当前 shell 默认 JDK 26 会触发 buildSrc Java/Kotlin target 不一致，需显式使用 JDK 25。
- 2026-05-22：Search ViewModel common 切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，并通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet`。
- 2026-05-22：History ViewModel common 切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，并通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet`。
- 2026-05-22：QuestionFeedViewModel common 切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，并通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet`。
- 2026-05-22：HomeFeedViewModel common 切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，并通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet`。
- 2026-05-22：LegacyLocalHistoryScreen common 切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，并通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet`。
- 2026-05-22：SearchScreen common 切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，并通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet`。
- 2026-05-22：OnlineHistoryScreen common 切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，并通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet`。
- 2026-05-22：CollectionContentScreen 页面主体已迁入 `shared/commonMain`，并删除对应整页 `expect/actual`；Android 侧只保留 `ArticleHost` provider、`AnswerNavigatorRepository` adapter、导出环境和返回键等小粒度平台能力。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，并通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet`。
- 2026-05-22：FollowScreen 和 FollowTopLevelPage 页面主体已迁入 `shared/commonMain`，并删除对应整页 `expect/actual`；刷新偏好和用户消息改用 common `SettingsStore` / `UserMessageSink`，屏蔽用户/主题等 Android 副作用收敛到 feed block action adapter。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，并通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet`。
- 2026-05-22：HomeScreen 页面主体已迁入 `shared/commonMain`，并删除对应整页 `expect/actual`；账号状态/登录、更新公告、外链、调试复制、本地推荐反馈、关键词屏蔽等 Android 副作用拆为 runtime / feed block adapter。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet`，并通过 HomeScreen common Android 边界 grep。
- 2026-05-22：QuestionScreen 页面主体已迁入 `shared/commonMain`，并删除对应整页 `expect/actual`；问题详情加载、历史/open event、WebView/Markdown 渲染、评论弹层、分享对话框和 Honor 选择菜单 workaround 拆为小粒度 Android/JVM adapter，分享文本 helper 迁入 common。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet`，并通过 QuestionScreen common 边界 grep；JDK 25 编译日志仍出现既有 `No class roots are found in the JDK path` 警告但任务成功。
- 2026-05-22：PinScreen 页面主体已迁入 `shared/commonMain`，并删除对应整页 `expect/actual`；旧 Android-only `PinViewModel` 已移除，想法详情加载、点赞请求、WebView/Markdown 渲染、分享/评论弹层、外链打开和 open event 记录收敛到 `PinScreenRuntime`。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet :app:runKtlintFormatOverAndroidTestSourceSet`，并通过 PinScreen common 边界 grep；JDK 25 编译日志仍出现既有 `No class roots are found in the JDK path` 警告但任务成功。
- 2026-05-22：PeopleScreen 页面主体和 People 分页 ViewModel 已迁入 `shared/commonMain`，并删除对应整页 `expect/actual`；profile 加载、关注/拉黑/推荐屏蔽、WebView 打开、头像预览、Toast 和 Android blocklist 写入收敛到 `PeopleScreenRuntime`。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet`，并通过 PeopleScreen common 边界 grep；JDK 25 编译日志仍出现既有 `No class roots are found in the JDK path` 警告但任务成功。
- 2026-05-22：DeveloperSettingsScreen 页面主体已迁入 `shared/commonMain`，并删除对应整页 `expect/actual`；开发者模式偏好、cookie 保存、登录校验、token 刷新、签名请求复制、网络/省电状态、Toast 和 Android TTS runtime 信息收敛到 `DeveloperSettingsRuntime`。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet`，并通过 DeveloperSettings common 边界 grep；JDK 25 编译日志仍出现既有 `No class roots are found in the JDK path` 警告但任务成功。
- 2026-05-22：AppearanceSettingsScreen 页面主体和 ColorPickerDialog 已迁入 `shared/commonMain`，并删除对应整页 `expect/actual`；设置读写复用 common `SettingsStore`，提示复用 `UserMessageSink`，主题写入和 WebView 自定义字体文件选择收敛到 runtime adapter。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet`，并通过 AppearanceSettings common 边界 grep；common 中剩余 `WebView` 命中仅为既有可见文案、设置 key/test tag 和平台字体 slot 名称，不是 JVM WebView 依赖。
- 2026-05-22：SystemAndUpdateSettingsScreen 页面主体已迁入 `shared/commonMain`，并删除对应整页 `expect/actual`；GitHub token、Nightly、遥测、防沉迷间隔等设置语义复用 common `SettingsStore`，更新状态与 APK 下载/安装、外链打开拆为 `SystemAndUpdateSettingsRuntime`。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet`，并通过 SystemAndUpdateSettings common 边界 grep；desktop/JVM/common 的 WebView grep 仅剩 Appearance 设置页既有文案和字体 slot 名称，不是 JVM WebView 依赖。
- 2026-05-22：AccountSettingScreen 页面主体已迁入 `shared/commonMain`，并删除对应整页 `expect/actual`；账号状态、profile 刷新、登录/扫码、退出登录、复制版本号、外链打开、主 tab 选择和更新消息收敛到 `AccountSettingsRuntime`。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet :app:runKtlintFormatOverAndroidTestSourceSet`，并通过 AccountSettingScreen common 边界 grep；desktop/JVM/common 的 WebView grep 仅剩 Appearance 设置页既有文案和字体 slot 名称，不是 JVM WebView 依赖。
- 2026-05-22：BlocklistSettingsScreen 页面主体已迁入 `shared/commonMain`，并删除对应整页 `expect/actual`；导入/导出、Android SAF/FileProvider、JVM 文件路径和数据库 builder 收敛到 `BlocklistSettingsRuntime`。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet`，并通过 BlocklistSettings common 边界 grep；JDK 25 编译日志仍出现既有 `No class roots are found in the JDK path` 警告但任务成功。
- 2026-05-22：BlockUserConfirmDialog 对话框 UI 已迁入 `shared/commonMain`；Android actual 只保留 `BlocklistManager` 写入和 Toast，JVM actual 复用同一确认框 UI。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，并通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet`。
- 2026-05-22：HighlightableSettingContainer、VerticalReadingProgressBar、simpleVerticalScrollbar 等纯 Compose 组件已迁入 `shared/commonMain`。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，并通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet`。
- 2026-05-22：AnswerHorizontalOverscroll 和 AnswerVerticalOverscroll 已迁入 `shared/commonMain`。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet`，并通过 common 边界 grep；`WebView` 命中仅为注释中描述传入 slot 内容，不是 common 依赖。
- 2026-05-22：ShareDialogContent 已迁入 `shared/commonMain`，Android `ShareDialogComponent` 只保留系统分享、剪贴板、偏好读取和导航副作用。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet`，并通过 ShareDialogContent common 边界 grep。
- 2026-05-22：HomeAccountSettingSheetContent 整页 expect/actual 转发壳已删除，HomeScreen 直接调用 common AccountSettingScreen。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet`，并通过 HomeAccountSettingSheetContent grep 确认已清除。
- 2026-05-22：OpenSourceLicensesContent 已迁入 `shared/commonMain`，并删除 Android/JVM 整块内容 actual；aboutlibraries KMP 依赖移动到 common，Android/JVM 仅提供 `Libs` 数据加载和 full/lite 判断 adapter。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，JDK 25 编译日志仍出现既有 `No class roots are found in the JDK path` 警告但任务成功。
- 2026-05-22：BlockByKeywordsDialog 外层状态机已迁入 `shared/commonMain`，并删除 Android/JVM 对话框整块 actual；Android 只提供关键词提取和 NLP 短语写入 runtime，JVM 提供空能力避免引入 Android/NLP 依赖。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，JDK 25 编译日志仍出现既有 `No class roots are found in the JDK path` 警告但任务成功。
- 2026-05-22：OpenImagePreviewContent 已迁入 `shared/commonMain`；Android `OpenImageDialog` 只保留平台弹窗宿主、Telephoto `ZoomableAsyncImage` renderer slot 和保存/分享/浏览器打开副作用。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet`，并通过 common Android/WebView/Telephoto 边界 grep；WebView grep 命中仅为既有设置文案/注释和字体 slot 名称，不是 JVM WebView 依赖。
- 2026-05-22：CommentScreenSheetContent 已迁入 `shared/commonMain`；Android `CommentScreenComponent` 只保留现有 Android `CommentScreen` slot 和 `AccountData.httpClient(context)`。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet`，并通过 CommentScreenSheetContent common 边界 grep。
- 2026-05-22：SegmentedText 已通过 `git mv` 迁入 `shared/commonMain`；Android `Context`、SharedPreferences、ClipData、`AccountData` 和 `signFetchRequest` 已拆入 `SegmentedTextRuntime.android`，JVM 提供剪贴板和本地点赞状态 fallback。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet`，并通过 SegmentedText common Android/WebView 边界 grep。
- 2026-05-22：评论 ViewModel 已通过 `git mv` 迁入 `shared/commonMain`；`BaseCommentViewModel` / `RootCommentViewModel` / `ChildCommentViewModel` 去掉 Android `Context`、Toast、`AccountData`、`signFetchRequest` 和 `htmlEncode` 依赖，改为通过 `PaginationEnvironment` 获取 HttpClient 和签名配置，并使用 common `ZhihuJson` 解码。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet`，并通过评论 ViewModel common Android 边界 grep。
- 2026-05-22：CommentScreen 页面主体已通过 `git mv` 迁入 `shared/commonMain`；Jsoup 改为 common Ksoup，Java 时间格式化改为 kotlinx-datetime/`kotlin.time.Clock`，图片与 emoji/Honor workaround 副作用拆入 `CommentScreenRuntime` Android/JVM actual。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew assembleLiteDebug`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet :app:runKtlintFormatOverAndroidTestSourceSet`，并通过 CommentScreen common Android/WebView 边界 grep；`:app:compileLiteDebugAndroidTestKotlin` 仍失败于既有迁移测试债务（多个设置页 internal test tag / API drift），本切片相关的 `YMDHMS` 兼容出口已恢复。
- 2026-05-22：CommentScreenComponent 已迁入 `shared/commonMain`；它现在只是 common `CommentScreenSheetContent` + common `CommentScreen` 的薄 wrapper，不再需要 Android `Context`、`AccountData.httpClient` 或平台 source set。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，并通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet`。
- 2026-05-22：ArticleScreen 中的纯 UI helper 已抽入 `shared/commonMain` 的 `ArticleUiCommon.kt`；投票颜色、`ScrollThresholdDp`、bottom-bar avoiding `BringIntoViewSpec` 和 `ArticleTopAppBar` 复用 common 实现，Android `ArticleScreen` 继续保持原 UI 调用结构。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，并通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet`。
- 2026-05-22：`ArticleSummarySheet` 已抽入 `shared/commonMain` 的 `ArticleUiCommon.kt`；文章总结 bottom sheet 的标题、加载、错误、可选择文本和按钮布局复用 common 实现，Android `ArticleScreen` 继续按原参数调用，不改变 UI 行为。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，并通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet`。
- 2026-05-22：文章发布时间格式化已从 Android `SimpleDateFormat` 改为 common `formatArticleDateTime`；`ArticleScreen` 不再为标题滚动阈值和文章日期保留 Android-local helper。
- 2026-05-22：文章更多操作菜单的 action button 视觉组件已抽入 common `ArticleMenuActionButton`；朗读、分享、复制、导出、浏览器发送等平台副作用仍留在 Android `ArticleActionsMenu` 回调内。
- 2026-05-22：文章更多操作菜单的分享/复制文本拼装已抽入 common `articleActionText`，Android 端只负责系统分享 Intent 和剪贴板副作用。
- 2026-05-23：文章朗读文本提取已抽入 common `articleSpeechText`，HTML 到纯文本解析改用 common Ksoup；Android `ArticleActionsMenu` 只保留 TTS 调用、协程调度和 Toast 错误提示。
- 2026-05-23：文章更多操作菜单的 bottom sheet 和按钮编排已抽入 common `ArticleActionsMenuSheet` / `ArticleActionsMenuContent`；Android `ArticleActionsMenu` 继续只提供 TTS、系统分享、剪贴板、导出和浏览器发送副作用。
- 2026-05-23：缓存回答预览的 Scaffold、作者信息、底栏和按钮布局已抽入 common `CachedAnswerPreviewContent`；Android wrapper 只传入投票图标资源和现有 `RenderMarkdown` 正文 slot。
- 2026-05-23：双击回答动作选择弹层已抽入 common `AnswerDoubleTapActionDialog`；Android 端只负责偏好保存、立即点赞/打开评论和 Toast 提示。
- 2026-05-23：文章时间格式化已保留在 common `formatArticleDateTime`；发布日期/编辑日期继续使用 `master` 同名本地函数 `DateTexts` 的结构，`IP属地` 继续在 Android `ArticleScreen` 原位置展示，不再抽无意义 UI 组件。
- 2026-05-23：文章顶部作者行已迁入 common `ArticleAuthorRow`；头像、作者名、徽章、简介和作者页跳转语义复用同一套 UI，跳转直接使用 shared `LocalNavigator`，Android `ArticleScreen` 只传入现有 `ArticleViewModel` 字段。
- 2026-05-23：文章底部操作栏已迁入 common `ArticleActionBarContent`；master 的 Button/duo3 分支、投票/收藏/朗读停止/评论/更多布局保持原结构，Android 只保留投票 drawable slot、ViewModel 投票回调、TTS 停止和弹层状态副作用。
- 2026-05-23：文章“下一个回答”悬浮按钮已迁入 common `ArticleSkipAnswerButton`；显示条件、自动隐藏 alpha 动画、加载态和 `DraggableRefreshButton` 位置偏好保持原逻辑，Android 只传入滚动/设置状态和 `navigateToNext` 副作用。
- 2026-05-23：文章回答切换外壳和阅读进度条已迁入 common `ArticleAnswerSwitchContainer`；竖向/横向回答切换、预览 slot 和进度条布局复用同一套 UI，Android 只保留横向预览 WebView 预加载副作用与缓存预览 renderer slot。
- 2026-05-23：文章正文区域布局已迁入 common `ArticleContentArea`；WebView/Markdown 双路径的日期位置、尾部媒体 slot、IP 属地和底部留白由 common 复用，Android 只提供 WebView/Markdown/video renderer slot 和 WebView 滚动恢复副作用。
- 2026-05-23：文章页偏好状态已拆成 common `ArticleScreenSettingsState` 和平台 `rememberArticleScreenSettingsState()`；Android `SharedPreferences` 监听与 JVM `SettingsStore` 读取留在平台 actual，`ArticleScreen` 主体不再直接注册偏好 listener 或保存双击动作偏好，为后续保持 master 大函数结构迁入 common 做准备。
- 2026-05-24：文章正文 noscript 图片修正逻辑已迁入 common `prepareContentDocument`，HTML 解析改用 common Ksoup；Android `ArticleScreen` 不再直接依赖 Jsoup `Document` 或 SDK 版本分支，只向 WebView slot 传入 common 处理后的 HTML 和图片失败 Toast 回调。
- 2026-05-24：文章阅读历史写入已拆成 common `ArticleReadHistoryRecorder` 和平台 `rememberArticleReadHistoryRecorder()`；Android actual 继续复用 `AccountData.addReadHistory`，JVM 先提供空实现，`ArticleScreen` 主体不再直接依赖 `AccountData`。
- 2026-05-24：文章页主函数和更多操作菜单内的直接 `Toast.makeText` 已替换为 common `UserMessageSink`；Android Toast 只保留在平台 sink actual，文章 UI 主体继续减少 Android 直接副作用。
- 2026-05-24：文章更多操作菜单的 TTS、系统分享、复制链接和浏览器打开副作用已拆入 `ArticleActionsRuntime` 平台 adapter；`ArticleActionsMenu` 保留原菜单 UI 结构和按钮顺序，但不再直接依赖 Android `Context`、`Intent`、`ClipData`、`OpenInBrowser` 或 `viewModelScope`。
- 2026-05-24：文章页滚动恢复日志改用 common `shared.util.Log`，`ArticleScreen` 不再直接 import Android `Log`。
- 2026-05-24：撤回文章正文日期/IP 属地 footer 的无语义微组件抽取；`ArticleContentArea` 继续在原 WebView/Markdown footer 位置内联日期和 `IP属地` 文本，避免 `ArticleIpInfoText` / `ArticleMetaTexts` 这类错误抽象方向。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet`，并通过错误抽象残留 grep 与 common Android/WebView 边界 grep。
- 2026-05-24：新增 common `ArticleViewModelRuntime` 窄接口和 Android runtime 实现，先把 `ArticleViewModel` 的内容详情加载、open event、回答导航仓库、收藏列表加载/新建、投票、AI summary 签名/xsrf 和消息提示接到 runtime；`ArticleViewModel` 函数名和主体流程保持接近 master，UI 未改动。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet`，并通过 `ArticleViewModelRuntime.kt` common 边界 grep；下一步继续把剩余 `Context` 形参、`MutableLiveData` 滚动状态、导出/权限/WebView 缓存拆离后再迁 ViewModel core。
- 2026-05-24：`ArticleViewModel.rememberedScrollY` 已从 Android `MutableLiveData(0)` 改为 Compose/common 可用的 `mutableIntStateOf(0)`，`ArticleScreen` 保持原滚动恢复位置和逻辑，只去掉 `.value` 容器访问；`permissionRequested` 仍暂留 Android 导出权限流程。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverAndroidMainSourceSet`，并通过 `rememberedScrollY.value` / `MutableLiveData(0)` 残留 grep。
- 2026-05-24：`ArticleViewModel.convertToMarkdown()` / `htmlToMarkdown()` 保留 master 函数名和递归结构，将 Android-only Jsoup 替换为 common 已使用的 Ksoup；文章 Markdown 导出转换逻辑继续留在原函数，减少后续迁 ViewModel core 的 Android 解析库阻塞。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverAndroidMainSourceSet`，并通过 ArticleViewModel `org.jsoup` / `Jsoup` 残留 grep。
- 2026-05-24：`ArticleViewModel` 已移除最后的 Android `MutableLiveData` 依赖；导出权限请求计数改为 `mutableIntStateOf`，权限请求调用点和 Toast 文案保持不变。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverAndroidMainSourceSet`，并通过 ArticleViewModel `MutableLiveData` / `LiveData` / `permissionRequested` 残留 grep。
- 2026-05-24：`ArticleViewModel` 的图片/HTML 导出权限检查、权限请求和导出 Toast 提示已接入 `ArticleViewModelRuntime`；原 `exportToHtml` / `exportToImageInternal` 函数名、WebView 渲染、MediaStore/Downloads 保存流程和成功提示时长保持不变，只把 Android `ActivityCompat` / `ContextCompat` / `PackageManager` 副作用下沉到 runtime adapter。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `git diff --check`，并通过 ArticleViewModel 权限/Toast 边界 grep；`exportToClipboard` 的剪贴板写入和 Toast 仍是下一步待拆平台副作用。
- 2026-05-24：`ArticleViewModel.exportToClipboard(context)` 保留 master 的函数名和“生成 Markdown -> 复制剪贴板 -> Toast”主体顺序，剪贴板写入已接入 `ArticleViewModelRuntime.copyArticleMarkdownToClipboard()`；Android `ClipData` / `clipboardManager` 只保留在 runtime adapter。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `git diff --check`，并通过 ArticleViewModel 剪贴板/Toast 边界 grep。
- 2026-05-24：`ArticleViewModel.parseSummaryErrorMessage()` 保留原函数结构，将 `AccountData.json` 替换为 common `ZhihuJson.json`，减少文章 ViewModel 对 Android 账号数据单例的 JSON 解析依赖。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverAndroidMainSourceSet`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `git diff --check`，并通过 ArticleViewModel `AccountData.json` 残留 grep。
- 2026-05-24：`ArticleViewModel.fetchExportComments()` 保留原函数名、数量裁剪和 `mapExportComment` 映射位置，评论导出请求、签名和 `DataHolder.Comment` 解码已接入 `ArticleViewModelRuntime.fetchExportComments()`；Android `rootCommentUrl` / `signFetchRequest` / `AccountData.decodeJson` 只保留在 runtime adapter。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `git diff --check`，并通过 ArticleViewModel 评论导出边界 grep。
- 2026-05-24：`ArticleViewModel.createOfflineHtmlContent()` 保留原 `httpClient ?: ...` fallback 结构，将 Android `AccountData.httpClient(context)` 替换为 `ArticleViewModelRuntime.accountHttpClient()`；`ArticleViewModel` 不再直接 import `AccountData`。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `git diff --check`，并通过 ArticleViewModel `AccountData` 边界 grep。
- 2026-05-24：`ArticleViewModel` 日志调用已从 Android `android.util.Log` 切换为 common `shared.util.Log` facade；日志 tag、调用位置和错误处理流程保持不变，继续减少文章 ViewModel 的 Android-only import。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverAndroidMainSourceSet`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `git diff --check`，并通过 ArticleViewModel `android.util.Log` 边界 grep。
- 2026-05-24：`ArticleViewModel.loadExportAssetText(context, fileName)` 保留原函数名、try/catch 和失败日志位置，将 Android `context.assets.open(fileName)` 读取下沉到 `ArticleViewModelRuntime.loadExportAssetText()`；导出 footnote 注入流程不变。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `git diff --check`，并通过 ArticleViewModel `assets.open` 边界 grep。
- 2026-05-24：`ArticleViewModel.saveHtmlToDownloads(context, htmlContent)` 保留原函数名和 `buildExportFileName("html")` 位置，Android MediaStore/legacy Downloads 写入已下沉到 `ArticleViewModelRuntime.saveHtmlToDownloads(displayName, htmlContent)`；ArticleViewModel 不再直接保留 HTML Downloads 保存 helper。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `git diff --check`，并通过 ArticleViewModel `MediaStore.Downloads` / `Environment.DIRECTORY_DOWNLOADS` / `java.io.File` 边界 grep。
- 2026-05-24：`ArticleViewModel.mapExportComment()` 保留原函数名和 `prepareArticleExportComment` 调用结构，将 Android/JVM-only `SimpleDateFormat` / `Date` 替换为 common `formatArticleDateTime(comment.createdTime).dropLast(3)`，继续保持导出评论时间格式为 `yyyy-MM-dd HH:mm`。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverAndroidMainSourceSet`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `git diff --check`，并通过 ArticleViewModel `SimpleDateFormat` / `java.util.Date` 边界 grep。
- 2026-05-24：`ArticleViewModel.saveImageToMediaStore(context, bitmap)` 保留原函数名和 `buildExportFileName("png")` 位置，Android MediaStore 图片写入、PNG 压缩和失败日志已下沉到 `ArticleViewModelRuntime.saveImageToMediaStore(displayName, bitmap)`；ArticleViewModel 不再直接 import `ContentValues` / `MediaStore` / `Environment`。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `git diff --check`，并通过 ArticleViewModel 图片 MediaStore 边界 grep。`Bitmap` 和 WebView 截图链仍在 Android source set，后续应作为完整导出 renderer adapter 继续拆离。
- 2026-05-24：`ArticleViewModel.exportToHtml()` 中的 Android `Build.VERSION` 判断已下沉到 `ArticleViewModelRuntime.requiresHtmlExportPermission()`；HTML 导出旧版存储权限流程、权限请求文案和 `onComplete(false)` 分支保持不变，ArticleViewModel 不再直接 import `android.os.Build`。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `git diff --check`，并通过 ArticleViewModel `Build.VERSION` 边界 grep。
- 2026-05-24：回答切换预览 WebView 的三槽缓存、tag、硬件加速偏好读取、`CustomWebView` 创建和销毁已从 `ArticleViewModel.ArticlesSharedData` 移入 Android-only `AndroidArticlePreviewWebViewStore`；`ArticlesSharedData` 继续保留 `ArticleAnswerSwitchState` 的导航器、pending content、`reset()` / `promoteForNavigation()` 语义，并通过委托实现 `ArticlePreviewWebViewStore`，ArticleScreen UI 调用点不变。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverAndroidMainSourceSet`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `git diff --check`，并通过 ArticleViewModel `CustomWebView` / `PREFERENCE_NAME` / WebView cache 字段边界 grep。
- 2026-05-24：图片导出 WebView 渲染、ready 轮询、footnote JS 注入、WebView measure/layout、Bitmap 捕获、WebView 销毁和 Bitmap recycle 已从 `ArticleViewModel` 移入 Android-only `AndroidArticleExportRenderer`；`ArticleViewModel.exportToImageInternal()` 继续保持 master 的 prepare -> capture -> save -> toast -> destroy 主流程和函数名 wrapper，ArticleScreen UI 调用点不变。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverAndroidMainSourceSet`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `git diff --check`，并通过 ArticleViewModel `WebView` / `Bitmap` / `Canvas` / `Handler` / `Looper` / `WebResourceRequest` renderer 边界 grep；后续继续处理 `Context` 形参、生命周期/NavBackStackEntry 和平台 runtime 注入。
- 2026-05-24：图片导出 renderer 类型已通过 common `ArticleImageExportRenderer` / `PreparedArticleExportContent` 接口隐藏，`ArticleViewModel` 不再直接引用 `AndroidPreparedExportWebView` 或 `AndroidArticleExportRenderer`，但 `exportToImageInternal()` 仍保持 master 的 prepare -> capture -> save -> toast -> destroy 主流程和函数名 wrapper。Android WebView/Bitmap 实现继续只在 `AndroidArticleExportRenderer` 内；切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `git diff --check`，并通过 ArticleViewModel Android renderer 类型边界 grep；后续继续处理剩余 `Context` 形参、`ArticlePreviewWebViewStore`、生命周期/NavBackStackEntry 和 runtime 注入。
- 2026-05-24：`ArticleViewModel` 构造器已移除 `NavBackStackEntry` / `LifecycleOwner` Android 类型依赖，滚动暂停同步语义改为由 Android 调用方注册 `onPause` 回调；`ZhihuMainAndroidContent` 仍在 Android source set 内把 back stack lifecycle pause 转发给 ViewModel，离线 ArticleScreen instrumented test 继续直接构造 ViewModel。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverAndroidMainSourceSet :app:runKtlintFormatOverMainSourceSet :app:runKtlintFormatOverAndroidTestSourceSet`，再次通过同一编译命令和 `git diff --check`，并通过 ArticleViewModel lifecycle/NavBackStackEntry 边界 grep；后续继续处理 `Context` 形参和 `ArticlePreviewWebViewStore`。
- 2026-05-24：`ArticleViewModel` 的导出 helper 已进一步改为接收 `ArticleViewModelRuntime` / `ArticleImageExportRenderer`，`loadExportAssetText`、`prepareExportWebView`、`captureExportBitmap`、`destroyExportWebView`、`recycleExportBitmap`、`saveImageToMediaStore`、`saveHtmlToDownloads` 不再自行从 `Context` 重建 runtime 或 renderer；`exportToImageInternal()` 和 `exportToHtml()` 的原函数名、调用顺序、保存位置和提示流程保持不变。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverAndroidMainSourceSet`，再次通过同一编译命令、`git diff --check` 和导出 helper `context` 残留 grep；后续继续处理对外 `Context` 形参、HTML builder 平台依赖和 `ArticlePreviewWebViewStore`。
- 2026-05-24：`ArticleViewModel.loadCollections(context)` 保留原 public 入口，内部新增 runtime 版本供 `toggleFavorite` / `createNewCollection` 复用，收藏成功后的刷新不再重新从 `Context` 构造 runtime；收藏排序、`collectionOrder`、错误日志和调用顺序保持不变。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverAndroidMainSourceSet`，再次通过同一编译命令和 `git diff --check`；后续继续收敛其他仍由 `Context` 创建 runtime 的入口。
- 2026-05-24：`ArticleViewModel.toggleFavorite(context)`、`requestAiSummary(context)`、`createNewCollection(context, ...)` 和 `toggleVoteUp(context, ...)` 保留原 public 入口与主体请求/解析/状态更新结构，内部新增同名 runtime 版本承载实际逻辑；这些函数的 private 主体不再直接接收 `Context`。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverAndroidMainSourceSet`，再次通过同一编译命令、`git diff --check` 和 private runtime 入口 `Context` 残留 grep；后续继续处理 `loadArticle(context)`、导出/HTML builder 外部入口和 `ArticlePreviewWebViewStore`。
- 2026-05-24：`ArticleViewModel.loadArticle(context)` 保留原 public 入口，主体新增 runtime 版本；回答/文章加载、字段映射、回答导航器初始化、历史写入和 open event 记录顺序保持原结构，`ArticleHost` 获取与历史写入已下沉到 `ArticleViewModelRuntime.articleAnswerSwitchState()` / `postHistoryDestination()`，ViewModel 不再直接调用 `context.articleHost()`。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet`，再次通过同一编译命令、`git diff --check` 和 ArticleViewModel `articleHost()` 残留 grep；后续继续处理 public `Context` 入口、HTML builder 平台依赖和 `ArticlePreviewWebViewStore`。
- 2026-05-24：`ArticleViewModel.exportToImage(context)`、`exportToImageWithComments(context, ...)`、`exportToHtml(context, ...)` 和 `exportToClipboard(context)` 保留原 public 入口，内部新增 runtime 版本/参数传递；图片导出、HTML 导出、评论导出和剪贴板复制的主流程、函数名和提示文案保持不变，`fetchExportComments` 与离线 HTML fallback 不再自行重建 runtime。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverAndroidMainSourceSet`，再次通过同一编译命令和 `git diff --check`；后续继续处理 HTML builder 的 Android `Context` 依赖、剩余 public `Context` 入口和 `ArticlePreviewWebViewStore`。
- 2026-05-24：`ArticleViewModel.createHtmlContent()` / `createOfflineHtmlContent()` 已改为通过 `ArticleViewModelRuntime.buildArticleExportHtml()` / `buildOfflineArticleExportHtml()` 生成 HTML；`ArticleViewModel` 不再直接 import `buildArticleExportData`、`buildArticleExportHtml` 或 `buildOfflineArticleExportHtml`，Android template/Context 读取继续留在 `AndroidArticleViewModelRuntime` 内复用原 util。导出 HTML 内容、图片内联、评论 section 和提示流程保持不变。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet`，再次通过同一编译命令、`git diff --check` 和 ArticleViewModel HTML util import/call 边界 grep；后续继续处理 `ArticlePreviewWebViewStore` 和 public `Context` 入口。
- 2026-05-24：回答切换的纯状态已从 Android `ArticleViewModel.ArticlesSharedData` 拆入 common `ArticleAnswerSwitchData`，包括 `navigator`、`pendingNavigator`、`pendingInitialContent`、`navigatingFromAnswerSwitch`、`answerTransitionDirection` 和 `reset()` 语义；Android `ArticlesSharedData` 继续保留原嵌套类名并继承 common 状态，只负责 `ArticlePreviewWebViewStore`、三槽 WebView cache、`promoteForNavigation()` 的 WebView 旋转、`reset()` 清 contentId 和 `onCleared()` 销毁 WebView。MainActivity/ArticleScreen 调用点不变，UI 不变。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet`，再次通过同一编译命令、`git diff --check` 和状态字段位置 grep；后续继续处理 Android 预览 WebView store 与 public `Context` 入口。
- 2026-05-24：`ArticleViewModel` 构造器新增默认 `runtimeProvider: (Context) -> ArticleViewModelRuntime`，内部 `articleRuntime(context)` 不再硬编码创建 `AndroidArticleViewModelRuntime(context)`；现有 Android 构造调用保持兼容，ViewModel 主体和 ArticleScreen UI 调用不变。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverAndroidMainSourceSet`，再次通过同一编译命令、`git diff --check` 和 Android runtime 创建点 grep；后续可逐步把 public `Context` 入口替换为 runtime 入口。
- 2026-05-24：`ArticleScreen` 为当前 `Context` 创建一次 `AndroidArticleViewModelRuntime`，加载文章/收藏、投票、AI summary、收藏夹新建/切换和 Markdown 剪贴板导出已改为调用 `ArticleViewModel` 的 runtime 入口；`ArticleViewModel` 保留原 public `Context` 入口兼容 Android 调用，新增公开 runtime 入口供页面复用，导出图片/HTML 仍保留 `Context` 作为 Android renderer 边界。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverAndroidMainSourceSet`，再次通过同一编译命令和 `git diff --check`；`ArticleScreen` 中业务 ViewModel 调用不再传裸 `context`，剩余 `context` 调用只在图片/HTML 导出边界。
- 2026-05-24：`ArticleViewModel` 的 runtime 图片/HTML 导出入口已移除冗余 `Context` 形参，`ArticleScreen` 调用 `exportToImage(articleRuntime)`、`exportToImageWithComments(articleRuntime)` 和 `exportToHtml(articleRuntime)`；原 public `Context` wrapper 继续保留，导出主流程、函数名、提示文案和 renderer 获取位置不变。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverAndroidMainSourceSet`，再次通过同一编译命令、`git diff --check` 和 ArticleScreen/ViewModel 裸 `context` 导出调用残留 grep。
- 2026-05-24：`ArticleViewModel` 已移除未被调用的 `Context -> ArticleViewModelRuntime` 构造器 provider、`articleRuntime(context)` helper 和各业务/导出 public `Context` wrapper；保留同名 runtime 入口及原主体流程，Android route 创建与离线 instrumented test 构造器调用保持兼容。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverAndroidMainSourceSet`，再次通过同一编译命令、`git diff --check` 和 ArticleViewModel `articleRuntime` / `runtimeProvider` / public Context wrapper 残留 grep；剩余 `Context` 仅来自 Android 预览 WebView store。
- 2026-05-24：`ArticleViewModel.ArticlesSharedData` 已收窄为纯 `ArticleAnswerSwitchData` 子类，回答切换预览 WebView 缓存、`Context`、`ArticlePreviewWebViewStore` 和 `AndroidArticlePreviewWebViewStore` 移入 Android-only `AndroidArticlesSharedData`；`MainActivity` 和 Android route transition 改为通过 `ViewModelProvider` 获取该 Android 子类，`reset()` / `promoteForNavigation()` 继续保留原语义并同步 WebView 缓存旋转/清理。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverAndroidMainSourceSet :app:runKtlintFormatOverMainSourceSet`，再次通过同一编译命令、`git diff --check`，并通过 `ArticleViewModel` Android `Context` / WebView store 依赖残留 grep。
- 2026-05-24：`ArticleExportComment`、`prepareArticleExportComment()`、`buildArticleExportCommentsHtml()`、`buildArticleExportFileName()`、`normalizeArticleExportUrl()` 和文件名/HTML escape helper 已迁入 `shared/commonMain`，供 `ArticleViewModel` 直接依赖 common 导出工具；Android `ArticleExportUtils.kt` 继续保留 asset template、Android `Context`、Jsoup Document 和图片 data URL 内联等平台侧导出能力。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :app:runKtlintFormatOverMainSourceSet`，通过 `git diff --check` 和 Android util 重复函数残留 grep；尝试运行 `:app:testLiteDebugUnitTest --tests com.github.zly2006.zhihu.export.ArticleExportHtmlTest` 时被既有 `AccountData.decodeJson` internal 可见性测试编译问题挡住，未进入该测试本体。
- 2026-05-24：`ArticleViewModel.kt` 已通过 `git mv` 从 `shared/androidMain` 迁入 `shared/commonMain`；文章加载、收藏、收藏夹新建、投票、AI summary、导出图片/HTML、Markdown 复制和 `ArticlesSharedData` 的关键函数名与主体流程继续对照 `master` 原函数保留，Android `AccountData`/签名请求、Toast、剪贴板、权限、HTML template、MediaStore/WebView renderer 仍通过 `ArticleViewModelRuntime` / `ArticleImageExportRenderer` 平台 adapter 承接。迁移后通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :app:runKtlintFormatOverMainSourceSet`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `git diff --check`，并通过 ArticleViewModel common Android API 边界 grep；`WebView` / `MediaStore` 命中仅为注释、方法名或平台 adapter 接口名，不是 common Android 依赖。
- 2026-05-24：`ArticleScreen.kt` 已通过 `git mv` 从 `shared/androidMain` 迁入 `shared/commonMain`，文章页面主体、底栏、回答切换容器、跳过回答按钮、正文 WebView/Markdown slot 编排和缓存回答预览复用同一套 Compose；Android/JVM 只通过 `ArticleScreenRuntime` / `ArticleViewModelRuntime` 提供 WebView、Markdown/video renderer、预览预加载、ArticleHost/TTS 和网络/导出等平台能力。为符合本轮迁移的 master 相似度要求，撤回 `ArticleContentArea`、`ArticleActionBarContent`、`ArticleAnswerSwitchContainer`、`ArticleSkipAnswerButton` 等无必要 helper 在 `ArticleScreen` 内的调用，并删除这些无调用抽象；`TwoRowsTopAppBar` 恢复 master 关键函数名。相似度检查结果：`ArticleActionsMenu` 0.809、`ArticleScreen` 0.804、`CachedAnswerPreview` 0.950。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet :app:runKtlintFormatOverMainSourceSet`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，通过 `git diff --check`，并通过 ArticleScreen common Android API 边界 grep；`withContext` 命中只是 coroutine helper，不是 Android `Context`。
- 2026-05-24：内容过滤入口继续 shared 化：新增 `ContentFilterEntries.kt`，把 `recordContentDisplay`、`recordContentInteraction`、`performMaintenanceCleanup`、`applyForegroundReadFilterToDisplayItems`、`applyContentFilterToDisplayItems` 这些入口语义放入 `shared/commonMain`，Android `ContentFilterExtensions` 只负责 `Context` 到 settings/database/detail provider/NLP matcher/Toast/Log 回调的薄适配，原 Android 调用方保持不变。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet`，通过内容过滤 JVM 测试和 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:jvmTest --tests com.github.zly2006.zhihu.viewmodel.filter.ContentFilterDatabaseTest --tests com.github.zly2006.zhihu.viewmodel.filter.FeedFilterSettingsTest --tests com.github.zly2006.zhihu.viewmodel.filter.ForegroundReadFilterPipelineTest --tests com.github.zly2006.zhihu.viewmodel.filter.FeedContentFilterPipelineTest --tests com.github.zly2006.zhihu.viewmodel.filter.FeedDisplayFilterPipelineTest :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`，并通过 filter common Android API 边界 grep；`currentCoroutineContext` 只是误中，不是 Android `Context`。
- 2026-05-24：desktop/JVM `PaginationEnvironment` 已接入同一套 shared 内容过滤入口，`applyHomeFeedFilters()` 不再直接 no-op；JVM 通过 `~/.zhihu-plus/settings.properties`、`~/.zhihu-plus/content-filter.db`、空详情 provider 和轻量 lexical semantic matcher 复用 common 的前台已读过滤、内容过滤、NLP 短语匹配和 interaction 记录语义。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:jvmTest --tests com.github.zly2006.zhihu.viewmodel.filter.ContentFilterDatabaseTest --tests com.github.zly2006.zhihu.viewmodel.filter.FeedFilterSettingsTest --tests com.github.zly2006.zhihu.viewmodel.filter.ForegroundReadFilterPipelineTest --tests com.github.zly2006.zhihu.viewmodel.filter.FeedContentFilterPipelineTest --tests com.github.zly2006.zhihu.viewmodel.filter.FeedDisplayFilterPipelineTest :shared:compileKotlinJvm :desktopApp:compileKotlin :app:compileLiteDebugKotlin --continue`；后续 `13832d36` 又补齐了 JVM 空 semantic matcher 残留。
- 2026-05-24：`RenderMarkdown.kt` 和 `MdAst.kt` 已通过 `git mv` 从 `shared/androidMain` 迁入 `shared/commonMain`，保留 `RenderImage`、`RenderVideoBox`、`RenderMarkdown`、`htmlToMdAst` 等关键函数名和主体结构；HTML AST 转换从 Android Jsoup 改为 common Ksoup，图片 URL 与划线段落解析复用 common util，图片预览/保存/分享、外链打开和数学字体下载收敛到 `MarkdownRuntime` 平台 adapter。JVM `ArticleMarkdownContent`、`QuestionDetailContent`、`PinHtmlContent` 不再退回纯 `Text` fallback，改为复用 common `RenderMarkdown`；desktop 详情数据加载 stub 仍是后续债务。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`，通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet`，通过 `git diff --check`，并通过 Markdown common Android API / JVM 纯文本 fallback 边界 grep。
- 2026-05-24：`ContentOpenEventSupport.recordOpenEvent()` 和 `getAlreadyOpenedContentIds()` 的数据库插入/查询编排已迁入 common `shared.filter.ContentOpenEventSupport`，Android `viewmodel.filter.ContentOpenEventSupport` 只负责 `Context -> getContentFilterDatabase(context)` 的薄适配；内容打开记录继续和 feed 曝光记录分离。对照 `master`，`buildContentKey`、`toTrackedContentIdentity`、`inferOpenFrom`、`filterUnopenedAnswerArticles`、`partitionQuestionAnswerCandidates` 保持 100% 结构相似，`recordOpenEvent` 为 0.821；`getAlreadyOpenedContentIds` 因去掉 Android `Context`/`Dispatchers.IO`/`ContentFilterDatabase.getDatabase(context)` 平台访问而低于 0.8，但保留了 idsToCheck -> DAO query -> toSet 主体顺序。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`；尝试运行 `:app:testLiteDebugUnitTest --tests com.github.zly2006.zhihu.viewmodel.filter.ContentOpenEventSupportTest` 时仍被既有 unit test 编译债挡住（`AccountData.decodeJson` / `peopleProfileUrl` internal 可见性），未进入目标测试本体。
- 2026-05-24：desktop/JVM `PinScreenRuntime.loadPinDetail()` 不再返回“暂时无法在桌面端加载想法详情”的占位错误，改为复用 `DesktopAccountStore` cookies 请求 `https://www.zhihu.com/api/v4/pins/{id}`，存在 `d_c0` 时使用 shared `signZhihuFetchRequest()` 签名，并通过 `ZhihuJson.decodeJson<DataHolder.Pin>()` 解析详情；`PinScreenUiState` 继续保持 master `PinViewModel.loadPinDetail()` 的 `pinContent`、`likeCount`、`isLiked` 填充语义和“无法加载想法详情”错误文案。对照 `master`，endpoint、签名请求、`DataHolder.Pin` 解码和状态填充结构保持一致；Android `AccountData.addReadHistory()` / `ContentOpenEventSupport.recordOpenEvent()` 仍是平台侧 read history/open event 能力，JVM 本切片只补齐详情加载，不伪造 Android history 副作用。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 JVM Pin placeholder 残留 grep。
- 2026-05-24：desktop/JVM `QuestionScreenRuntime.loadQuestion()` 不再返回 `null` 占位，改为复用 `DesktopAccountStore` cookies 请求 master/Android 同一问题详情 endpoint（`read_count,visit_count,answer_count,voteup_count,comment_count,follower_count,detail,excerpt,author,relationship.is_following,topics`），存在 `d_c0` 时用 shared `signZhihuFetchRequest()` 签名，继续保留 master `DataHolder.getContentDetail(question)` 的 `id` 转 Long 兼容处理和 `DataHolder.Question` 解码；加载成功后按 Android runtime 同一字段填充 `QuestionScreenUiState` 并构造 `Question(questionId, title)` 作为 history destination。Android read history、open event 和 WebView 打开日志仍是平台副作用，JVM 只补齐详情数据和桌面浏览器打开日志页。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 JVM Question placeholder 残留 grep。
- 2026-05-24：desktop/JVM `DesktopArticleViewModelRuntime.getContentDetail()` 不再返回 `null` 占位，改为复用 master/Android `DataHolder.getContentDetail(article)` 的 answer/article endpoint、`id` 转 Long 兼容处理和 `DataHolder.Answer` / `DataHolder.Article` 解码；请求仍走当前 runtime 的 `fetchGet()` / `configureSignedRequest()`，由 `DesktopAccountStore` 提供 cookies 和 `d_c0` 签名。`ArticleViewModel.loadArticle()` 主体、函数名和 UI 调用点未改，仍按原结构填充回答/文章状态；Android open event/history/WebView/TTS 继续是平台 adapter，JVM 本切片只补齐详情数据。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 Article JVM placeholder 残留 grep。
- 2026-05-24：desktop/JVM `AnswerNavigatorRepository` 不再返回空回答导航数据：`fetchAnswerContent()` 复用 `DesktopArticleViewModelRuntime.getContentDetail()`，`fetchQuestionFeeds()` 复用 Android repository 的 `questions/{id}/feeds?limit=6` 入口并解码 `Feed` 列表和 `paging.next`，`fetchCollectionItems()` 解码收藏夹 item 列表和下一页，`getAlreadyOpenedAnswerIds()` / `recordOpenEvent()` 接入同一个 `~/.zhihu-plus/content-filter.db` KMP Room 数据库。`QuestionAnswerNavigator`、`CollectionAnswerNavigator`、`PaginationInfoNavigator` 的主体和 UI 预览逻辑未改；JVM 端回答切换/预取开始复用 shared 导航语义，Android 的 `Context` / `ContentDetailCache` / `AccountData` 仍仅在 Android adapter。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 AnswerNavigator JVM 空实现残留 grep；`AnswerNavigatorPage(emptyList(), "")` 命中仅为请求失败/缺 data fallback，不是固定空实现。
- 2026-05-24：desktop/JVM `PinScreenRuntime.fetchLinkCardPreview()` 不再返回固定 `null`，改为按 Android `fetchAndroidLinkCardPreview()` 同一结构解析 `resolveLinkCardDestination()`，并分别复用桌面 article/question/pin 详情加载生成关联卡片标题和预览文本；`compactTitle()` / `compactPreview()`、链接卡片 UI 和 `PinScreen` 主体均保持 shared 原结构。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 JVM Pin link-card 固定空实现残留 grep。
- 2026-05-24：desktop/JVM `DesktopArticleViewModelRuntime.fetchExportComments()` 不再固定返回 `emptyList()`，按 Android runtime 同一结构请求 `article.rootCommentUrl`，设置 `order=score`、`limit=min(requestedCount, 20)`、`include=data[*].content,excerpt,headline` 并签名，解码 `DataHolder.Comment` 后 `take(requestedCount)`；`ArticleViewModel.fetchExportComments()`、`mapExportComment()`、导出 HTML 主流程和 UI 调用点未改。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 JVM export comments 固定空实现残留 grep。
- 2026-05-24：desktop/JVM 文章、问题和想法详情入口已接入知乎 read history 上报：`ArticleReadHistoryRecorder.jvm`、`QuestionScreenRuntime.loadQuestion()` 和 `PinScreenRuntime.loadPinDetail()` 复用 shared `addZhihuReadHistory()`，用 `DesktopAccountStore` cookies 与 `d_c0` 签名提交和 Android `AccountData.addReadHistory()` 同一语义的 content token/type。页面主体、导航和 UI 未改；没有新增本地 history 存储，只补齐 Web read history 副作用。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 JVM read-history no-op 残留 grep。
- 2026-05-24：desktop/JVM 本地历史接入 `DesktopHistoryStorage`，沿用 Android `HistoryStorage` 的 `history.json` / `List<NavDestination>` 序列化格式，最多保留 1000 条并按最近优先返回；`DesktopPaginationEnvironment.localHistory()` / `clearAllHistory()` 不再使用 common 默认空实现，`DesktopArticleViewModelRuntime.postHistoryDestination()`、`QuestionScreenRuntime.loadQuestion()`、`PinScreenRuntime.loadPinDetail()` 会写入文章/问题/想法历史。`HistoryViewModel`、`OnlineHistoryViewModel` 和页面 UI 主体未改。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 JVM localHistory/clearAllHistory 默认空实现残留 grep；common interface 默认空实现保留为未实现平台 fallback，不是 JVM 实现。
- 2026-05-24：desktop/JVM `ArticleVideoAttachmentContent()` 不再是固定 `Unit`，改为和 Android actual 同一主体结构解析 `attachment.type == "video"`、`attachmentId` 和 `video.videoInfo.thumbnail`，并复用 common `RenderVideoBox()` 渲染视频附件；`ArticleScreen` 页面主体、正文 footer 调用点和 Android UI 未改。对照 Android actual / `master` 原视频附件片段，函数主体相似度为 1.000。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 JVM `ArticleVideoAttachmentContent = Unit` 残留 grep。
- 2026-05-24：desktop/JVM `PeopleScreenRuntime` 不再只回填姓名或原样返回关注/屏蔽状态，改为复用 Android actual 同一 profile endpoint、include 字段、`DataHolder.People` 解码、徽章映射、follower/following/answer/article count 填充、关注/取消关注 POST/DELETE、拉黑/取消拉黑 POST/DELETE 和推荐屏蔽本地 blocklist 语义；加载个人页时同时上报 read history 并写入 `DesktopHistoryStorage`。`PeopleScreen` 页面主体、列表分页 ViewModel、按钮文案和 Android UI 未改；平台差异只在 cookies/sign、浏览器打开图片/链接、Toast/本地 DB 文件路径。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`。
- 2026-05-24：desktop/JVM `DesktopArticleViewModelRuntime.saveHtmlToDownloads()` 不再只返回文件名，改为和 Android legacy Downloads 路径同一职责实际创建 `~/Downloads/Zhihu++` 并写入 HTML 文件，返回真实文件绝对路径；`ArticleViewModel.exportToHtml()`、导出菜单 UI 和 Android MediaStore/Downloads 实现未改。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`。
- 2026-05-24：desktop/JVM `rememberOpenSourceLicensesLibraries()` 不再固定返回空 `Libs`，改为优先读取 classpath `aboutlibraries.json`，开发运行时 fallback 到 app 生成的 `app/build/generated/aboutLibraries/{lite,full}Debug/res/raw/aboutlibraries.json` 或 packaged res，再通过 `Libs.Builder().withJson()` 构建许可列表；`OpenSourceLicensesScreen` / `OpenSourceLicensesContent` UI 和 Android `withContext(context)` 数据加载未改。若资源或生成文件缺失才保留空列表 fallback。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`。
- 2026-05-24：desktop/JVM `rememberFeedBlockActions()` 不再提供固定 no-op，改为和 Android actual 保持同一 `FeedBlockActions` 入口结构：屏蔽用户读取 feed/raw author 后复用 common `BlockUserConfirmDialogContent`，屏蔽主题写入同一个 `~/.zhihu-plus/content-filter.db` KMP Room blocklist 并移除当前 feed 中同主题内容，按关键词屏蔽复用 common `BlockByKeywordsDialog` 与 `KeywordAnalyzerCore.extractFromFeedWithWeight()`，JVM 侧用轻量 regex extractor 和 `BlockedKeywordService.addNLPPhrase()` 持久化短语。Android UI/common UI 未改；对照 Android actual，保留 `rememberFeedBlockActions`、`rememberBlockByKeywordsRuntime`、`BlockUserConfirmDialog` 关键函数名和回调顺序，平台差异仅为 Toast/Context/Android manager 替换为 JVM 数据库文件与控制台提示。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`，通过 `git diff --check` 和 JVM feed block no-op 残留 grep。
- 2026-05-24：desktop/JVM `ArticleActionsRuntime.toggleSpeech()` 不再是固定 `Unit`，改为沿用 Android actual 的 `toggleSpeech -> articleSpeechText(title, content) -> speak/stop` 主流程：JVM runtime 在检测到本地 `say` 命令时把 `ttsState` 置为 `Ready`，朗读时通过 stdin 传入 common 提取后的正文，停止时销毁当前进程并回到 `Ready`；不可用时显式 `Error`，不引入 Android `TextToSpeech` / `Context`。`ArticleScreen`、`ArticleActionsMenu` 和按钮 UI 未改；对照 Android actual，保留 `rememberArticleActionsRuntime`、`toggleSpeech`、`shareArticle`、`copyArticleLink`、`openArticleInBrowser` 关键函数名和调用顺序，平台差异仅为 Android TTS engine/Toast/Intent 替换为 JVM `say` / clipboard/browser adapter。本机验证 `command -v say` 为 `/usr/bin/say`；切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 JVM `toggleSpeech = Unit` 残留 grep。
- 2026-05-24：desktop/JVM `ArticleImageExportRenderer` 不再抛出 “Desktop image export renderer is not implemented”，`hasImageExportPermission()` 改为桌面端始终允许，图片导出继续走 common `ArticleViewModel.exportToImageInternal()` 的 prepare -> capture -> save -> toast -> destroy 主流程；JVM adapter 用 Swing `JEditorPane` 将 `buildArticleExportHtml()` 生成的 HTML 渲染为 `BufferedImage`，`saveImageToMediaStore()` 写入 `~/Downloads/Zhihu++/{displayName}`。Android WebView/Bitmap renderer 和 ArticleScreen UI 未改；对照 Android renderer，保留 `prepareExportWebView`、`captureExportBitmap`、`destroyExportWebView`、`recycleExportBitmap` 和 `saveImageToMediaStore` 关键函数名，平台差异仅为 WebView 截图替换为桌面 HTML renderer。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 JVM 图片导出未实现残留 grep。
- 2026-05-24：desktop/JVM 内容过滤不再把 NLP semantic matcher 固定为空：`DesktopPaginationEnvironment.applyHomeFeedFilters()` 和 `FeedBlockActions.jvm` 的 `BlockedKeywordService` 现在使用同一轻量 lexical matcher，短语全文命中记为 1.0，短语 token 覆盖率按原 `KeywordSemanticMatcher` threshold 判定；这让桌面端通过 `BlockByKeywordsDialog` 保存的 NLP 短语能参与后续 shared feed filtering。Android full/lite NLP variant 仍由 Android adapter 注入，desktop 不引入 `sentence_embeddings` 或 Android 依赖；UI 未改。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 JVM 空 semantic matcher 残留 grep。
- 2026-05-24：`rememberAccountSettingsPlatformRuntime()` 的 Android/JVM actual 不再直接返回 `noopSettingsStore()` / 空 `UserMessageSink`，改为注入同一套平台 `rememberSettingsStore()` 和 `rememberUserMessageSink()`；`rememberCommonAccountSettingsRuntime()` 继续保持原有 copy 合并结构，账号页 UI、登录/退出/复制/外链/主 tab 回调未改。这样直接使用 platform runtime 的路径也能复用 shared settings/message 能力，desktop 写入 `~/.zhihu-plus/settings.properties`，Android 继续走平台 SharedPreferences adapter。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 AccountSettingsRuntime noop 残留 grep。
- 2026-05-24：desktop/JVM 评论 emoji inline content 不再固定 `emptyMap()` / `null` key，改为复用仓库现有 `misc/emoji_mapping.json` 和 `misc/emojis/*.png` 缓存，按 Android 同一规则生成 `emoji_<fileName>` inline key，并用 Compose `InlineTextContent` / `Image` 渲染 PNG；评论页面主体、AnnotatedString 构造和 Android `EmojiManager` 未改。当前 JVM 实现优先服务开发/本地运行目录，后续若要发布独立 desktop 包，还需把 emoji mapping/png 纳入 desktop 资源打包。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 JVM comment emoji 空实现残留 grep。
- 2026-05-24：desktop/JVM `DeveloperSettingsRuntime` 不再把 cookies、verify login、save cookies 和 signed GET 固定为空实现：开发者模式开关接入 desktop `SettingsStore`，cookies/verify/save 复用 `DesktopAccountStore` 和 shared profile verification，签名 GET 使用 `DesktopAccountStore` cookies 与 `signZhihuFetchRequest(dc0)` 请求后复制到系统剪贴板；网络状态、消息提示也给出桌面端实际 runtime 文案/adapter。Android 开发者页 runtime 和 UI 未改；桌面端 token refresh 仍暂不实现，避免重写 Android-only `ZhihuCredentialRefresher`。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 DeveloperSettingsRuntime JVM 空实现残留 grep。
- 2026-05-24：`ZhihuCredentialRefresher` 已从 Android-only util 迁入 `shared/commonMain`，保留 `fetchRefreshToken()`、`refreshZhihuToken()`、`generateRefreshPayload()` 的函数名、请求 endpoint、header、payload 顺序和返回 token 语义；只把 Android/JVM-only 的 HMAC-SHA1 和 UTF-8 byte conversion 拆成小粒度 `hmacSha1Hex()` 平台 actual，并用 Ktor common `encodeURLParameter(spaceToPlus = true)` 替代 `URLEncoder`。desktop/JVM `DeveloperSettingsRuntime.refreshToken` 现在复用 common refresher 和 `DesktopAccountStore` cookies，不再是空实现；Android 调用点和 UI 未改。master 对照文件为 `app/src/main/java/com/github/zly2006/zhihu/util/ZhihuCredentialRefresher.kt`，本切片保留关键函数名和 `fetch -> refresh -> sign_in` 主体结构，平台拆分点仅为 `Mac` / `SecretKeySpec` / `StandardCharsets` / `URLEncoder`。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check`、common Java crypto 残留 grep 和 JVM `refreshToken = {}` 残留 grep；包含 Android ktlint 的完整命令曾在 `:shared:runKtlintFormatOverAndroidMainSourceSet` 因既有 KSP 生成目录 `LocalContentDao_Impl.kt (No such file or directory)` 失败，编译任务已通过。
- 2026-05-24：desktop/JVM `rememberHomeScreenRuntime()` 不再硬编码 `isLoggedIn = true`，也不再把登录入口、外链和调试数据复制固定成空回调；账号登录态改为读取 `DesktopAccountStore` 的 cookie/session，点击登录入口会重新读取本地登录备份，QQ群等外链用桌面浏览器打开，调试 JSON 复制到系统剪贴板。JVM `isDebuggable` 改为 true，让 shared 首页复用 Android debug 版同一个 `DraggableRefreshButton` / `CopyAll` 调试复制入口；`HomeScreen`、feed UI、按钮文案和 Android actual 未改。本地推荐点击/反馈仍保持 JVM no-op，因为 `LocalHomeFeedViewModel` 和本地推荐学习仍在 Android adapter，不在本切片伪造。对照 Android actual，保留 `rememberHomeScreenRuntime` 和 `HomeScreenRuntime` 字段结构，平台差异只在 `Context`/`Intent`/Android clipboard/debuggable flag 替换为 desktop account store、`Desktop.browse`、AWT clipboard 和 JVM 开发入口显示。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 JVM home runtime 空回调/硬编码登录态残留 grep。
- 2026-05-24：desktop/JVM `NotificationScreenData.copyDebugData` 不再是空回调，改为和 Android actual 同一语义编码 `viewModel.debugData`，写入系统剪贴板并提示“已复制调试数据”；`NotificationDebugCopyButton` 不再空实现，按 Android actual 同一函数名和 `DraggableRefreshButton` / `CopyAll` 图标结构显示调试复制悬浮按钮，通知列表加载、筛选、已读操作和 common 页面 UI 未改。对照 `master` 的 `NotificationScreen`，保留调试复制按钮、`Json.encodeToString(viewModel.debugData)`、复制后提示等关键结构；平台差异仅为 Android `ApplicationInfo.FLAG_DEBUGGABLE` / `ClipData` 替换为 JVM 开发入口显示和 AWT clipboard。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 JVM notification `copyDebugData = {}` / 空 `NotificationDebugCopyButton` 残留 grep。
- 2026-05-24：desktop/JVM `SystemUpdateRuntime` 不再把自动检查更新固定为关闭且忽略写入，改为复用 desktop `SettingsStore` 按 Android 同名 key `autoCheckUpdates` 持久化开关；关闭自动检查时同样重置为 `NoUpdate`。系统/更新设置页 UI、外链打开、检查更新占位和 APK 下载/安装不支持语义未改，后者仍是 Android 发行平台能力，不能在 desktop 伪造。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 JVM system update 空 auto-check setter 残留 grep。
- 2026-05-24：desktop/JVM `rememberUserMessageSink()` 不再吞掉所有 shared 页面消息，`showShortMessage` / `showLongMessage` 先按当前 JVM runtime 既有模式输出到控制台；Android Toast actual、shared 页面调用点和可见 UI 结构未改。这样 Account/Home/Appearance/ContentFilter/Blocklist/Notification/Article 等已迁 common 页面在桌面端至少不再丢失操作反馈；后续若要做桌面原生 toast/snackbar，可在不改页面调用点的前提下替换这个平台 sink。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 JVM `showShortMessage = {}` 残留 grep。
- 2026-05-24：desktop/JVM `BlocklistSettingsRuntime.requestImport` 不再显示“桌面端暂不支持导入文件”，改为通过桌面文件选择器选择 json/txt 文件，读取后复用 common `BlocklistService.importAllBlocklistFromJsonText()` 导入并回调原 `BlocklistSettingsScreen` 的成功提示与刷新逻辑；导出仍写入 `~/.zhihu-plus-plus/zhihupp_blocklist.json`。`BlocklistSettingsScreen` UI、Android SAF import/export 和 common `BlocklistService` 未改；平台差异仅为 Android `ActivityResultContracts.OpenDocument` 替换为 JVM `JFileChooser`。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 JVM 导入不支持占位残留 grep。
- 2026-05-25：desktop/JVM `PinScreenRuntime.toggleLike` 不再是空回调，改为对照 Android actual 同一 `toggleLike -> HttpMethod.Post/Delete -> /api/v4/pins/{id}/voters/up -> PinLikeResult` 主流程，用 `DesktopAccountStore` cookies、desktop HttpClient 和 common `signZhihuFetchRequest(dc0)` 发起请求并回填 `liked_count`；`PinScreen` 页面主体、点赞按钮 UI、分享弹层占位和 Android actual 未改。对照 Android actual，保留 `rememberPinScreenRuntime`、`toggleLike`、`handleShareAction`、`fetchLinkCardPreview` 关键字段和调用顺序，平台差异仅为 Android `Context`/`AccountData.fetch`/Toast 替换为 JVM account store、Ktor request 和控制台异常输出。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`:shared:runKtlintFormatOverJvmMainSourceSet`、`git diff --check` 和 JVM pin 点赞空回调残留 grep；完整 `./gradlew ktlintFormat` 因本轮不处理的 iOS compile 既有 blocker 失败，未作为本切片门禁。
- 2026-05-25：desktop/JVM `PinCommentsSheet` 不再是空函数，改为和 Android actual 同一形状直接调用已迁入 common 的 `CommentScreenComponent(showComments, onDismiss, content)`；`PinScreen` 评论按钮、sheet 触发状态、评论页面主体和 Android actual 未改。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 JVM `PinCommentsSheet` 空实现残留 grep。
- 2026-05-25：desktop/JVM `PinShareDialog` 不再是空函数，改为复用 common `ShareDialogContent` 显示和 Android 同一分享弹层 UI；分享/复制按钮在桌面端写入系统剪贴板并通过 `UserMessageSink` 提示，设置按钮继续用 shared `LocalNavigator` 跳转 `Account.AppearanceSettings(setting = "shareAction")`。`PinScreen` 分享按钮、`showShareDialog` 状态、common 分享弹层视觉结构和 Android actual 未改；平台差异仅为 Android 系统 share sheet/clipboard/Toast 替换为 JVM clipboard/message sink。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 JVM `PinShareDialog` 空实现残留 grep。
- 2026-05-25：desktop/JVM `QuestionCommentsSheet` 和 `QuestionShareDialog` 不再是空函数，分别复用 common `CommentScreenComponent` 与 `ShareDialogContent`，使问题页评论弹层和分享弹层在桌面端显示同一套 shared UI；分享/复制按钮写入桌面剪贴板并通过 `UserMessageSink` 提示，设置按钮沿用 shared `LocalNavigator` 跳转分享设置。`QuestionScreen` 主体、分享按钮触发状态、评论 sheet 触发状态和 Android actual 未改；平台差异仅为 Android WebView/share sheet/Toast 替换为 JVM markdown/clipboard/message sink。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 JVM `QuestionCommentsSheet` / `QuestionShareDialog` 空实现残留 grep。
- 2026-05-25：desktop/JVM 想法页和问题页 `handleShareAction` 不再固定弹出分享对话框，改为读取同一个 shared `shareActionMode` 设置：`ask` 保持弹层，`copy` 直接复制链接文本，`share` 在桌面端复制分享文本并提示。`PinScreen` / `QuestionScreen` 主体、分享按钮、分享弹层 UI 和 Android actual 未改；平台差异仅为 Android 系统 share sheet 替换为 JVM clipboard/message sink。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 JVM 固定 `onShowDialog()` 分享处理残留 grep。
- 2026-05-25：经 `gpt-5.5 xhigh` 独立边界审查（记录在 `docs/superpowers/reviews/2026-05-25-local-recommendation-boundary-review.md`），确认不能只给 JVM 首页 `recordLocalItemOpened` / `recordLocalItemFeedback` 补半截 DB 写入；本切片先把本地推荐学习核心 `UserBehaviorAnalyzer` 从 `shared/androidMain` 迁入 `shared/commonMain`，构造参数改为 `LocalContentDao`，保留 `recordBehavior`、`recordContentOpened`、`recordRecommendationFeedback`、`buildBehaviorProfile`、`getUserPreferences` 主体语义；Android 只保留 `Context -> getLocalContentDatabase(context).contentDao()` 薄 factory。新增 JVM 测试覆盖 click/like/dislike 行为写入和 profile multiplier。`HomeScreen` UI、JVM feedback 空回调、`LocalRecommendationEngine` 和 Android 本地推荐行为未改；下一步再审/迁 engine 和 ViewModel。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:runKtlintFormatOverJvmTestSourceSet :shared:jvmTest --tests com.github.zly2006.zhihu.viewmodel.local.UserBehaviorAnalyzerTest :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 common `UserBehaviorAnalyzer` 平台依赖残留 grep。
- 2026-05-25：本地推荐 `LocalContentIdentity.toNavDestination` 已迁入 `shared/commonMain` 的推荐语义；`RankedLocalResult`、`getFreshnessWeight`、`getDefaultWeight`、`createTaskForReason` 已迁入 `shared/commonMain` 的 `viewmodel.local` helper，保留 master 原函数名、分支、权重、URL 和 priority 主体。Android `LocalRecommendationEngine` 仍负责 DB/network/scheduler/context 副作用，调用同名 common helper；新增 common/JVM 测试覆盖导航目的地、时间桶、默认权重和任务模板。切片通过目标 JVM 测试、`:shared:compileKotlinJvm`、`:desktopApp:compileKotlin`、`:shared:compileAndroidMain`、`:app:compileLiteDebugKotlin`、targeted ktlint 和 `git diff --check`。
- 2026-05-25：本地推荐 `FeedGenerator` 已通过 `git mv` 从 `shared/androidMain` 迁入 `shared/commonMain`，构造参数改为 `LocalContentDao`，保留 `generateFeedFromResult`、`generateFeedsFromResults`、`generateFeedId`、`generateNavDestination`、`getReasonDisplayText` 的函数名、调用顺序和主体结构；Android 只保留 `Context -> getLocalContentDatabase(context).contentDao()` 薄 factory。新增 JVM 测试覆盖生成 feed 时保留已有 feedback/createdAt 和推荐理由文案。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:jvmTest --tests com.github.zly2006.zhihu.viewmodel.local.FeedGeneratorTest :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、targeted ktlint、`git diff --check` 和 common `FeedGenerator` Android/Context/System.currentTimeMillis 残留 grep。
- 2026-05-25：本地推荐 `rankCandidate`、`collectCandidateResults`、`ensurePendingTasks` 已从 Android `LocalRecommendationEngine` 迁入 `shared/commonMain` 同包 helper，保留 master 原函数名、候选 contentId 归一化、reason/content/freshness 权重、候选去重、每类任务补齐到 3 个的主体结构；Android engine 继续只负责调用这些 helper，并保留 network/connectivity/log/scheduler/context 副作用。新增 common/JVM 测试覆盖候选收集去重、排序 reasonDisplay 和任务补齐。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:jvmTest --tests com.github.zly2006.zhihu.viewmodel.local.LocalRecommendationSupportTest :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、targeted ktlint、`git diff --check` 和 common 本地推荐 Android/Context/ConnectivityManager/Log/AccountData/Toast/AlertDialog/System.currentTimeMillis 残留 grep。
- 2026-05-25：本地推荐 `LocalRecommendationEntry`、`buildFallbackRecommendations`、`toRecommendationEntry` 和 `cleanupLocalRecommendationData` 已迁入 `shared/commonMain`，保留 master 的兜底 recent results -> normalize -> rank -> sort -> take -> entry 映射流程，以及 completed/failed 任务保留 1 周、result/feed/behavior 保留 1 个月的数据清理策略；Android `LocalRecommendationEngine` 仍保留网络执行、connectivity、Log、scheduler 和 Context 副作用。新增 common/JVM 测试覆盖兜底 entry 生成和清理保留期。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:jvmTest --tests com.github.zly2006.zhihu.viewmodel.local.LocalRecommendationSupportTest :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、targeted ktlint、`git diff --check` 和 common 本地推荐 Android/Context/ConnectivityManager/Log/AccountData/Toast/AlertDialog/System.currentTimeMillis 残留 grep。
- 2026-05-25：本地推荐 `waitForTaskCompletion` 和 `insertRefreshTasks` 已迁入 `shared/commonMain`，保留 master 的 InProgress 任务轮询等待、500ms 检查间隔、刷新时按 reason 在 pending 少于 2 时插入任务的主体结构；Android `LocalRecommendationEngine` 仍负责实际 `crawlingExecutor.executeTask` 网络副作用、connectivity、Log、scheduler 和 Context。新增 common/JVM 测试覆盖无 InProgress 时等待返回和刷新任务插入阈值。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:jvmTest --tests com.github.zly2006.zhihu.viewmodel.local.LocalRecommendationSupportTest :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、targeted ktlint、`git diff --check` 和 common 本地推荐 Android/Context/ConnectivityManager/Log/AccountData/Toast/AlertDialog/System.currentTimeMillis 残留 grep。
- 2026-05-25：Android `TaskScheduler.cleanupOldData()` 已改为复用 shared `cleanupLocalRecommendationData(dao)`，删除调度器内重复的 `System.currentTimeMillis` retention 阈值和 DAO 清理调用；对照 `master` 的 `TaskScheduler`，保留 `startScheduling`、`stopScheduling`、`executePendingTasks`、`cleanupOldData` 函数名、调度循环、每次执行 pending tasks 后清理的调用顺序和 Android `CrawlingExecutor(context)` 副作用，只把已迁入 common 的清理策略集中复用。切片运行 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:ktlintAndroidMainSourceSetFormat :shared:runKtlintFormatOverCommonMainSourceSet :shared:jvmTest --tests com.github.zly2006.zhihu.viewmodel.local.LocalRecommendationSupportTest :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`，其中 JVM 测试和编译任务通过，Android Main ktlint 仍命中既有 KSP 生成文件缺失 blocker；另通过 `git diff --check` 和 `TaskScheduler` 重复清理残留 grep。
- 2026-05-25：本地推荐 `CrawlingExecutor` 内的纯 helper `createCrawlingResult`、`isVoteupFeed`、`extractQuestionIdFromUrl` 已迁入 `shared/commonMain`，保留 master 的函数名、target -> contentId/title/summary/url/score 映射、点赞 brief/attachedInfo 判断和 `question/(\\d+)` 正则；Android executor 仍负责 `Context`、`AccountData.fetchGet`、JSON decode、任务状态写入和网络执行副作用，只调用同名 common helper。新增 common/JVM 测试覆盖 feed target 映射、点赞动态判断和问题 URL 提取。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverCommonTestSourceSet :shared:jvmTest --tests com.github.zly2006.zhihu.viewmodel.local.LocalRecommendationSupportTest :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 common helper Android/Context/AccountData/System.currentTimeMillis 残留 grep。
- 2026-05-25：本地推荐初始化任务模板 `createFollowingTasks`、`createTrendingTasks`、`createDefaultUpvotedQuestionTasks`、`createQuestionFeedTask`、`createFollowingUpvoteTasks`、`createCollaborativeFilteringTasks` 和 `extractQuestionIdFromContentId` 已迁入 `shared/commonMain`，保留 master 的 URL、offset、reason、priority 和旧 contentId 数字 fallback 规则；Android `LocalContentInitializer` 仍保留 `initializeIfNeeded`、`checkDatabaseHasSufficientData`、`initializeFromHistory`、各 `initialize*Tasks`、`ensureSufficientTasks` 和 `createUpvotedQuestionTasks` 的原主体结构，继续只负责 `Context -> dao`、历史行为读取和任务入库。新增 common/JVM 测试覆盖任务模板与 contentId 解析。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverCommonTestSourceSet :shared:ktlintAndroidMainSourceSetFormat :shared:jvmTest --tests com.github.zly2006.zhihu.viewmodel.local.LocalRecommendationSupportTest :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 common helper Android/Context/AccountData/System.currentTimeMillis 残留 grep。
- 2026-05-25：本地推荐 `createLocalFeedDisplayItem` 已迁入 `shared/commonMain`，保留 master 的 `LocalRecommendationEntry -> FeedDisplayItem` 字段映射：title、summary、reasonDisplay、nav destination、local content/feed/reason metadata；Android `LocalHomeFeedViewModel` 仍保持 `recommendations.map(::createLocalFeedDisplayItem)` 调用形状，继续负责 `Context`、`AlertDialog`、`Log`、`viewModelScope` 和 fallback 内容。新增 common/JVM 测试覆盖本地推荐 metadata 与 nav JSON。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverCommonTestSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:jvmTest --tests com.github.zly2006.zhihu.viewmodel.local.LocalRecommendationSupportTest :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 common helper Android/Context/AccountData/System.currentTimeMillis 残留 grep。
- 2026-05-25：本地推荐 `LocalRecommendationEngine` 已通过 `git mv` 迁入 `shared/commonMain`，构造参数改为 `LocalContentDao`、`FeedGenerator`、`UserBehaviorAnalyzer` 和最小平台副作用回调，保留 master 的 `initialize`、`generateRecommendations`、`recordContentOpened`、`recordRecommendationFeedback`、`refreshContent`、`getRecommendationStream`、`cleanup`、`executeHighPriorityTasks` 主体流程和调用顺序；Android 只保留 `Context -> dao/generator/analyzer/initializer/scheduler/executor` 薄 factory、网络状态和 Log adapter。`applyReasonDiversity` 同步迁入 common 并保留 master 的 diversity target、每 reason 上限和补齐流程。新增 common/JVM 测试覆盖 engine 单次初始化和刷新任务执行优先级。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:jvmTest --tests com.github.zly2006.zhihu.viewmodel.local.LocalRecommendationSupportTest :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、targeted ktlint、`git diff --check` 和 common 本地推荐 Android/Context/ConnectivityManager/Log/AccountData/Toast/AlertDialog/Room.databaseBuilder/java 残留 grep。
- 2026-05-25：本地推荐 `LocalHomeFeedViewModel` 已通过 `git mv` 迁入 `shared/commonMain`，保留 master 的 `loadMore`、`fetchFeeds`、`onLocalItemOpened`、`onLocalItemFeedback`、`ensureEngine`、`generateFallbackContent` 主体结构、fallback 文案和负反馈移除逻辑；`Context`、`AlertDialog` 和 `Log` 改为 `PaginationEnvironment.localRecommendationEngine()`、`handleLocalRecommendationFailure()`、`showLocalRecommendationDatabaseError()` 小粒度平台能力。Android `SharedAndroidPaginationEnvironment` 注入真实 `LocalRecommendationEngine(context)`、数据库错误弹窗和日志；`HomeScreenRuntime.android` 只同步移除 Context 参数。桌面端不伪造本地推荐反馈成功，默认 environment 仍返回空 engine，避免半截 DB 写入。切片通过 targeted ktlint、`JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:jvmTest --tests com.github.zly2006.zhihu.viewmodel.local.LocalRecommendationSupportTest :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 common `LocalHomeFeedViewModel` / `PaginationEnvironment` Android/Context/Log/Toast/AlertDialog/AccountData 残留 grep。
- 2026-05-25：本地推荐 `TaskScheduler` 已通过 `git mv` 迁入 `shared/commonMain`，保留 master 的 `startScheduling`、`stopScheduling`、`executePendingTasks`、`cleanupOldData` 函数名、调度循环、每分钟检查、出错后等待 5 分钟、最多执行 3 个 pending task 和清理调用顺序；构造参数改为 `LocalContentDao` 与 `executeTask` 回调。Android 只保留 `Context -> dao` 和 `CrawlingExecutor(context)` 薄 factory。切片运行 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverAndroidMainSourceSet :shared:jvmTest --tests com.github.zly2006.zhihu.viewmodel.local.LocalRecommendationSupportTest :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`，其中 common ktlint、JVM 测试、JVM/desktop/Android 编译和 app lite Kotlin 编译通过，AndroidMain ktlint 仍命中既有 KSP 生成文件缺失 blocker；另通过 `git diff --check` 和 common `TaskScheduler` Android/Context/CrawlingExecutor/AccountData/Log/Toast/AlertDialog/System.currentTimeMillis/java 残留 grep。
- 2026-05-24：desktop/JVM `rememberCollectionScreenData()` 不再只返回测试数据和空 `loadMore`，改为按 Android actual 同一结构使用 common `CollectionsViewModel` 与 `DesktopPaginationEnvironment`，在真实 `urlToken` 下自动 refresh 并支持滚动分页；`CollectionScreen` UI、item 点击和 Android actual 未改。测试集合路径仍保持本地传入数据，避免破坏现有 preview/test 语义。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 JVM collection `loadMore = {}` 残留 grep。
- 2026-05-24：desktop/JVM 文章 HTML/图片导出不再只把正文和评论片段直接拼接，`DesktopArticleViewModelRuntime` 现在优先读取同一份 `article_export_template.html` 并填充标题、作者、头像、赞同数、评论数、正文、评论 section、导出/发布/编辑日期和 GitHub attribution 占位符；资源读取先查 classpath，再 fallback 到开发 worktree 的 `app/src/main/assets`。`ArticleViewModel.exportToHtml()` / `exportToImageInternal()` 主流程、菜单 UI 和 Android actual 未改；本切片当时仍缺图片 data-url 内联，后续条目已补齐。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 JVM 导出正文直拼残留 grep。
- 2026-05-24：desktop/JVM `buildOfflineArticleExportHtml()` 不再忽略传入的 `HttpClient`，改为对照 Android `buildOfflineArticleExportHtml -> inlineArticleExportImagesInHtml -> fetchArticleExportImageDataUrl` 主流程，用 Ksoup 预处理正文图片/链接，按 URL 去重并发下载图片，转换为 `data:<mime>;base64,...` 后写回 HTML；下载失败时保留原 URL，避免一次图片失败导致整篇导出失败。`ArticleViewModel.exportToHtml()`、导出菜单 UI、Android Jsoup/asset/MediaStore 实现未改；平台差异仅为 Android Jsoup/URLConnection 路径替换为 JVM Ksoup/Java MIME 推断。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 JVM 离线导出直通普通 HTML 残留 grep。
- 2026-05-24：common `StringOrListSerializer` 不再在序列化时抛 `NotImplementedError`，改为用 JSON array/null 编码 highlight 字段；搜索结果解码、`SearchViewModel`、页面 UI 和 Android/JVM runtime 未改。这个修复消除 shared 数据模型在调试/缓存/测试编码路径上的跨平台硬崩溃点。切片通过 `JAVA_HOME=$(/usr/libexec/java_home -v 25) ./gradlew :shared:runKtlintFormatOverCommonMainSourceSet :shared:runKtlintFormatOverJvmMainSourceSet :shared:compileKotlinJvm :desktopApp:compileKotlin :shared:compileAndroidMain :app:compileLiteDebugKotlin --continue`、`git diff --check` 和 SearchResult `NotImplementedError` 残留 grep。
